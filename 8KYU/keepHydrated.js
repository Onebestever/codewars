function litres(time) {
  let litres = Math.floor(0.5*time)
  return litres;
}

/*you can get liters by passing in time as an argumenr 
mulitply time which would be hour * 0.5 how many liters are consumed within an hour
then you would have to round down based on test */